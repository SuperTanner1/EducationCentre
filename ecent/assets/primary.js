import React from 'react';
import { Container, CssBaseline, Typography} from '@mui/material';

export default function Primary() {
    return (
        <Container maxWidth={"xl"}>
            <Typography variant="h1">Hello students</Typography>
        </Container>
    )
}